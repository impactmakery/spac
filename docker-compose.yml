services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: tah
      POSTGRES_PASSWORD: tah
      POSTGRES_DB: tah
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
