# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart          # multipart uploads (FastAPI needs it at runtime)

# Database
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pgvector>=0.3

# Config & auth
pydantic-settings>=2.4
email-validator>=2.0      # required by pydantic EmailStr
bcrypt>=4.2
pyjwt>=2.9

# AI: any OpenAI-compatible provider (OpenAI, OpenRouter, …)
openai>=1.40
tiktoken>=0.7             # chunking at 800 tokens / 150 overlap

# Document text extraction, one library per format
pypdf>=4.0
python-docx>=1.1
python-pptx>=0.6
openpyxl>=3.1

# Storage, email, monitoring
boto3>=1.34               # Cloudflare R2 (S3-compatible)
resend>=2.0
sentry-sdk[fastapi]>=2.0

# dev
ruff
mypy
pytest
pytest-asyncio
pgserver>=0.1.4           # embedded Postgres for local dev/tests without Docker
